services:
  traefik:
    image: traefik
    volumes:
      - $PWD/traefik.toml:/etc/traefik/traefik.toml:z
      - /var/run/docker.sock:/var/run/docker.sock:z
    ports:
      - "80:80"
      - "443:442"
